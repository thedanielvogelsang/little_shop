<h1> <%= @course.title %> </h1>
  <%= image_tag @course.image %>
<div>
  <p><%= @course.description %></p>
</div>
<%= button_to "Add to Cart", carts_path(course_id: @course.id), class: "btn btn-default" %>
