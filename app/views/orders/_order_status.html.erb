<% orders.each do |order| %>
  <% if order.status == ("paid" || "completed" ) %>
    <%= order.status %>
  <% else %>
    <%= order.updated_at.strftime("%A %D %H:%M %p") %>
  <% end %>
<% end %>

<!-- should this be just order.updated_at
why only if paid or completed??
if order.create_at != order.updated_at
order.updated_at -->
